{% assign lastone = page.author.last %}{% for person in page.author %}{{ person.family }}, {% assign firstnames = person.given | split:" " %}{% for name in firstnames %}{{ name | slice: -1, 1}}. {% endfor %}{% unless person == lastone %}, {%endunless %}{% endfor %}. ({{ site.published | date: "%Y" }}). {{ page.title }}. <i>{{ page.container-title }}, in PMLR</i> {{ site.volume }}:{{page.firstpage}}-{{page.lastpage}}

